{% extends "base.html.twig" %}
{% block stylesheets %}
{{ parent() }}
<style>
.buttonDelete
{
    position: absolute;
    
}
</style>
{% endblock %}
{% block body %}
<div class="container-fluid">
    <div class="container">
        <div class="row">
            <div class="col-12 shadow-sm">
                {{ form_start(form) }}
                {{ form_widget(form.pictures, {'attr': {'class': 'd-none'}}) }}
                {{ form_widget(form) }}
                <input type="submit" class="btn btn-outline-primary mt-3 mb-3" value="Modifier">
                {{ form_end(form) }}
            </div>
            <div class="col-12">
                <div class="row mt-3" data-id="{{ id }}" id="Display">

                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% block javascripts %}
{{ parent() }}
{{ encore_entry_script_tags('ads_edit') }}
{% endblock %}