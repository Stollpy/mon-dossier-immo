{% extends "base.html.twig" %}

{% block body %}
<div class="container-fluid">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <nav class="navbar navbar-expand navbar-light bg-transparent">
                    <div class="container-fluid">
                        <div class="collapse navbar-collapse" id="navbarNav">
                            <ul class="navbar-nav">
                                <li class="nav-item">
                                <a class="nav-link" aria-current="page" href="{{ path('user.edit', {'id' : app.user.id}) }}">Changer mes informations de compte</a>
                                </li>
                                <li class="nav-item">
                                <a class="nav-link" href="{{ path('user.edit-password', {'id' : app.user.id}) }}">Changer mon mot de passe</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>
</div>
{% block functionality %}{% endblock %}
{% endblock %}