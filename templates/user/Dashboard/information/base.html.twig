{% extends "base.html.twig" %}
{% block stylesheets %}{% endblock %}
{% block body %}
<div class="container-fluid">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <nav class="navbar navbar-expand navbar-light bg-transparent">
                    <div class="container-fluid">
                        <div class="collapse navbar-collapse" id="navbarNav">
                            <ul class="navbar-nav">
                                <li class="nav-item">
                                    <a href="{{ path('tenant.edit', {'id' : app.user.id}) }}" class="nav-link"> Mes informations locataire</a>
                                </li>
                                <li class="nav-item">
                                    <a href="{{ path('seller.edit', {'id' : app.user.id}) }}" class="nav-link"> Mes informations vendeur</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('document.edit', { 'id': app.user.id }) }}">Mes documents</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('user.garant', { 'id': app.user.id }) }}">Mes garants</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="{{ path('income.edit', { 'id': app.user.id }) }}">Mes revenues</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </nav>
            </div>
        </div>
    </div>
</div>
{% block functionality %}{% endblock %}
{% endblock %}
{% block javascripts %}{% endblock %}