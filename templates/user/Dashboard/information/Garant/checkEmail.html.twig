{% extends "base.html.twig"%}
{% block body %}
<div class="container-fluid">
    <div class="container">
        <div class="row">
            <div class="col-12">
                <h3 class="mt-3">Rentrez le code de sécurité envoyé par e-mail</h3>
            </div>
            <div class="col-12" class="mt-3">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" class="btn btn-outline-primary mt-3" value="confirmé">
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}